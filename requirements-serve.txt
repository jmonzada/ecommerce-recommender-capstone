# Serving-only dependencies (the Docker image installs this subset; the full
# research environment is requirements.txt). Frozen to the exact versions the
# evaluation ran with.
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.2.0
scipy==1.17.1
pyyaml==6.0.3
joblib==1.5.3
fastapi==0.139.0
uvicorn==0.50.0

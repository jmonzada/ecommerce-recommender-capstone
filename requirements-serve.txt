# Serving-only dependencies (the Docker image installs this subset; the full
# research environment is requirements.txt). Both get exact pins via
# `pip freeze` before the release tag, per docs/deployment_guide.md.
pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
xgboost>=2.0
scipy>=1.11
pyyaml>=6.0
joblib>=1.3
fastapi>=0.110
uvicorn>=0.29

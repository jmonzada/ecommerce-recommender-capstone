# Frozen to the exact versions the project was built and evaluated with
# (Python 3.11, Windows; pinned at submission per docs/deployment_guide.md)

# Core
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.2.0
scipy==1.17.1
pyyaml==6.0.3
joblib==1.5.3

# Explainability & fairness
shap==0.51.0
lime==0.2.0.1
fairlearn==0.14.0

# Visualisation
matplotlib==3.11.0
seaborn==0.13.2
umap-learn==0.5.12

# Notebooks (ipykernel runs them in VS Code; nbconvert is kept for notebook export.
# The full `jupyter` metapackage is intentionally omitted: its ipywidgets assets
# exceed Windows' 260-char path limit unless long-path support is enabled.)
ipykernel==7.3.0
nbconvert==7.17.1

# Data acquisition
kaggle==2.2.3

# Deployment & MLOps (Step 8)
fastapi==0.139.0
uvicorn==0.50.0
mlflow==3.14.0
pytest==9.1.1
httpx==0.28.1
ruff==0.15.20

# Generative AI (Step 9)
anthropic==0.116.0
python-dotenv==1.2.2

# Demo media generation (app/make_demo_video.py; the GIF script needs Pillow only)
imageio==2.37.3
imageio-ffmpeg==0.6.0

# Presentations
python-pptx==1.0.2
